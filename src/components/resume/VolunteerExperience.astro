---
import type { VolunteerExperience } from "../../types/resume";
import ResumeSection from "./ResumeSection.astro";

export interface Props {
	volunteerExperience: VolunteerExperience;
}

const { volunteerExperience } = Astro.props;

const volunteerItems = [volunteerExperience];

const getDate = (item: VolunteerExperience) =>
	`${item.startDate}–${item.endDate}`;

const getOrganization = (item: VolunteerExperience) => `${item.organization}`;

const getPosition = (item: VolunteerExperience) => item.position;

const getLocation = (item: VolunteerExperience) => item.location;

const getResponsibilities = (item: VolunteerExperience) =>
	item.responsibilities;
---

<ResumeSection 
  title="VOLUNTEER EXPERIENCE"
  items={volunteerItems}
  getDate={getDate}
  getOrganization={getOrganization}
  getPosition={getPosition}
  getLocation={getLocation}
  getResponsibilities={getResponsibilities}
/>
