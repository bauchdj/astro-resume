---
import type { VolunteerExperience as VolunteerExperienceType } from "../../types/resume";
import ResumeSection from "./ResumeSection.astro";

export interface Props {
	volunteerExperience: VolunteerExperienceType;
}

const { volunteerExperience } = Astro.props;

const volunteerItems = [volunteerExperience];

const getDate = (item: VolunteerExperienceType) => `${item.startDate}–${item.endDate}`;

const getInfo = (item: VolunteerExperienceType) => `${item.position} | <i>${item.organization}</i> | ${item.location}`;

const getResponsibilities = (item: VolunteerExperienceType) => item.responsibilities;
---

<ResumeSection 
  title="VOLUNTEER EXPERIENCE"
  items={volunteerItems}
  getDate={getDate}
  getInfo={getInfo}
  getResponsibilities={getResponsibilities}
/>
