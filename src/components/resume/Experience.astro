---
import type { ExperienceItem } from "../../types/resume";
import ResumeSection from "./ResumeSection.astro";

export interface Props {
	experience: ExperienceItem[];
}

const { experience } = Astro.props;

const getDate = (item: ExperienceItem) => `${item.startDate}–${item.endDate}`;

const getOrganization = (item: ExperienceItem) => `${item.company}`;

const getPosition = (item: ExperienceItem) => item.position;

const getLocation = (item: ExperienceItem) => item.location;

const getResponsibilities = (item: ExperienceItem) => item.responsibilities;
---

<ResumeSection 
  title="EXPERIENCE"
  items={experience}
  getDate={getDate}
  getOrganization={getOrganization}
  getPosition={getPosition}
  getLocation={getLocation}
  getResponsibilities={getResponsibilities}
/>
